version: '3.8'

services:
  ml-service-blue:
    image: ml-service:v1.0.0
    ports:
      - "8081:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
